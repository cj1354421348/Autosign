# AutoSign Environment Variables Template
# Copy this file to .env and adjust values

# Server Configuration
HOST=0.0.0.0
PORT=8000
SECRET_KEY=change_this_to_a_secure_random_string

# Database
# DB_URL=sqlite:///./data/autosign.db

# Admin User (Created on first run)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Notify Hub Configuration (Client Push)
# The URL where AutoSign sends task results.
# If running inside Docker and Notify Hub is on host, use host.docker.internal or real IP
NOTIFY_API_URL=http://127.0.0.1:8000/api/notify
NOTIFY_KEY=my-fixed-secret-key-123
